# Obtain your token from https://genius.com/api-clients
GENIUS_ACCESS_TOKEN=your_token_here

# Set to true to run as a streamable HTTP server (port 8000).
# Set to false to run in stdio mode (for Claude Desktop).
STREAMABLE_HTTP=true
